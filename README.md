# Java-SignUpForm
# Sign Up Form – Java Swing A simple **Sign Up Form** built in **Java Swing**. This project demonstrates GUI design, event handling, and file persistence in Java. User details (username, phone number, CNIC, and password) are saved into a text file (`users.txt`).
